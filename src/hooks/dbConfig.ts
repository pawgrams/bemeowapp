import {provider} from '../utils/provider';import {epbase} from '../utils/epbase';import {cluster} from '../utils/cluster';import {tkn} from '../utils/tkn';export const dbConfig = async () => {return {apiKey: tkn[2],authDomain: `${epbase[1]}.${provider[1]}`,projectId: epbase[1],storageBucket: `${epbase[1]}.${cluster[1]}`,messagingSenderId: tkn[3],appId: `1:${tkn[3]}:web:${tkn[4]}`,measurementId: tkn[5]};};